# teardown.md

Run before ending a **long** session.

1. Append session summary to `memory/runtime/dailylog.md`
2. Update `memory/runtime/context.md` “Next” section
3. If a decision was made, one line in `memory/runtime/key-decisions.md`
