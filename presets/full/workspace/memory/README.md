# memory/ (root)

Place **dated** operational logs here: `YYYY-MM-DD.md` for workspace-level notes.

Per-agent daily logs live under `agents/<slug>/memory/`.

**Routine:** read **today + yesterday** only. Archive older files periodically.
