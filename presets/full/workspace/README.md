# Full preset workspace

Multi-agent **markdown handoffs**: `agents/research` writes `intel/DAILY-INTEL.md`; `agents/content` and `agents/newsletter` write under `drafts/`. Root lead handles `HEARTBEAT.md` and distilled memory.

Copy from the template repo with `scripts/copy-preset.ps1 -Preset full` (or duplicate this folder). See the repo root `README.md` and `docs/INVARIANTS.md`.
