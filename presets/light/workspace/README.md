# Light preset workspace

Single example agent under `agents/example-agent/` plus root playbooks and `shared-context/` stubs. Easiest way to start one repetitive workflow.

Copy with `scripts/copy-preset.ps1 -Preset light`. See the template repo `README.md`.
